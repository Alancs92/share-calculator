{
  "name": "@share-calculator/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "share-calc": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@share-calculator/core": "*",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "vitest": "^4.1.9"
  }
}
